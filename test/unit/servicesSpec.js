'use strict';

/* jasmine specs for services go here */

describe('service', function() {
  beforeEach(module('myApp.services'));

  describe('when searching twitter', function() {
  	it('should send the search query to twitter using $http')	
  })
});
